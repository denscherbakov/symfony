<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('home') }}">
        <i class="fa fa-microchip"></i>
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('home') }}">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin/users_list') }}">Users</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin/posts_list') }}">Posts</a>
            </li>
        </ul>
        <form class="form-inline">
            {% if (is_granted('IS_AUTHENTICATED_FULLY')) %}
                <a href="{{ path('app_logout') }}" class="btn btn-outline-success my-2 my-sm-0">Logout</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="btn btn-outline-success my-2 my-sm-0">Login</a>
            {% endif %}
        </form>
    </div>
</nav>